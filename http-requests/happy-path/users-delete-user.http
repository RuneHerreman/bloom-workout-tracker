@baseUrl = http://localhost:8080/api/users
@token = REPLACE_WITH_JWT
@userId = REPLACE_WITH_USER_GUID

### Delete user (cascades to templates and logs)
DELETE {{baseUrl}}/{{userId}}
Authorization: Bearer {{token}}
